import React from 'react';

export default function Header() {
  return (
    <header className="absolute top-0 left-0 right-0 z-40 p-4 sm:p-6">
      {/* Header is now empty - buttons moved below hero */}
    </header>
  );
}